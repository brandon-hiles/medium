import React from 'react';
import Background from './Background';
import renderer from 'react-test-renderer';

test('Background is initially is white', () => {
    // insert test case
});

test('Background changes to blue', () => {
    // insert test case
});

test('Background changes to green', () => {
    // insert test case
});

test('Background change to rgba(250, 120, 210, 0.7)', () => {
    // insert test case
});

test('Background changes to #D5A232', () => {
    // insert test case
});